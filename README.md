# Conduit

A native iOS *bring-your-own-agent voice client*: point it at a voice agent you already run
(Pipecat / Daily / LiveKit over WebRTC) and talking to it becomes a hands-free phone call,
routed through your car over Bluetooth via CallKit. No backend of ours, no accounts, no
per-minute cost. See [the docs](./docs/README.md) for the architecture and full vision.

## Getting started

> **The Xcode project is generated by [XcodeGen](https://github.com/yonaskolb/XcodeGen) and is
> NOT committed.** You must run `xcodegen generate` after cloning (and after any change to
> `project.yml` or after adding/removing source files).

```sh
# 1. Prerequisites
brew install xcodegen          # Xcode 26+ also required

# 2. Per-developer build settings (team id + bundle id)
cp Config/local.xcconfig.template Config/local.xcconfig
#   …then edit Config/local.xcconfig with your DEVELOPMENT_TEAM and APP_BUNDLE_IDENTIFIER.
#   This file is gitignored. Simulator builds work without it; device builds need it.

# 3. Generate and open the project
xcodegen generate
open Conduit.xcodeproj
```

Build/test from the command line (or let an agent use XcodeBuildMCP):

```sh
xcodebuild -scheme Conduit \
  -destination 'platform=iOS Simulator,name=iPhone 16,OS=18.6' test
```

> **Simulator pin:** unit/UI tests run on **iPhone 16 / iOS 18.6**. The iOS 26 simulator has a
> broken accessibility subsystem that times out XCUITest queries — see the note in
> `.github/workflows/tests.yml`. The core CallKit + WebRTC live call is **device-only** and
> cannot run in the simulator.

## Project layout

```
Conduit/
  App/        App entry point and root view
  Core/       Shared models, services, utilities (e.g. Log, AccessibilityID)
  Features/   Feature modules (Views / ViewModels / Services / Models)
  Shared/     Reusable components and navigation helpers
  Resources/  Fonts, JSON, assets
Config/       Base.xcconfig (committed) + local.xcconfig (gitignored, per-developer)
docs/         Architecture, conventions, core-systems guides (start at docs/README.md)
project.yml   XcodeGen project definition (the .xcodeproj is generated from this)
```

## More

- **Working in this repo (humans & agents):** [`CLAUDE.md`](./CLAUDE.md) and [`AGENTS.md`](./AGENTS.md)
- **Architecture & conventions:** [`docs/README.md`](./docs/README.md)
